# Deploy Flask App on Render

The **app** serves as a main menu that directs user to different applications.

## Files
```
/
├── app.py              # Flask backend
├── requirements.txt    # List of dependencies
├── render.yaml         # Optional: Blueprint for Render (or configure via UI)
└── templates/
    └── index.html      # Your mobile GUI
```